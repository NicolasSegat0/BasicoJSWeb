const num1 = 10; 
const num2 = 100;

var soma = num1 + num2; 
console.log(soma);

var sub = num2 - num1; 
console.log(sub); 

var mult = num1 * num2;
console.log(mult); 

var div = num2 / num1; 
console.log(div);

alert("Soma:" + soma + "\nSubtração:" + sub + "\nMultiplicação:" + mult + "\nDivisão:" + div);